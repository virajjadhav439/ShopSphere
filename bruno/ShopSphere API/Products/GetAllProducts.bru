meta {
  name: GetAllProducts
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/products?sort=newest
  body: none
  auth: bearer
}

params:query {
  sort: newest
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: true
  timeout: 0
}
