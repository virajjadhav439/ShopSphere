meta {
  name: GetAllProducts
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/products?page=1&limit=6&search=vivo
  body: none
  auth: bearer
}

params:query {
  page: 1
  limit: 6
  search: vivo
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: true
  timeout: 0
}
