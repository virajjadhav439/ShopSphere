meta {
  name: GetAllCategories
  type: http
  seq: 2
}

get {
  url: http://localhost:3000/api/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{JWT}}
}

settings {
  encodeUrl: true
  timeout: 0
}
